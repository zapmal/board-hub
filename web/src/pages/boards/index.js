import React from 'react';
import styled from 'styled-components/macro';
import {
  Typography,
} from '@material-ui/core';

const Boards = () => {
  return (
    <>
      <Typography variant='h4' color='secondary'>
        Aquí verás todos tus tableros.
      </Typography>
    </>
  );
};

export default Boards;