import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#f6914d',
      light: '#f7a770',
      dark: '#ac6535'
    },
    secondary: {
      main: '#ffffff'
    },
  }
});

export default theme;