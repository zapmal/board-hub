/**
 * - Fills the form.
 * - Duplication (name) check (?).
 * - Token.
 * - Insert the board, we need the user's id.
 * - Done, lmao.
 */
import boardsRoutes from './boardsRoutes';

export { boardsRoutes };